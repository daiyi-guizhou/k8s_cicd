# K8s Console — 本地网关部署指南

> **目标**: 通过域名 `http://k8s-cicd.daiyi.local.com:9001` 访问本地 K8s 集群中的前后端应用。

---

## 一、完整链路架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         用户浏览器 (Windows / Chrome)                      │
│            http://k8s-cicd.daiyi.local.com:9001                          │
│            hosts: 127.0.0.1 → k8s-cicd.daiyi.local.com                  │
│            代理绕过: *.daiyi.local.com                                    │
└────────────────────────────┬────────────────────────────────────────────┘
                             │ DNS 解析 → 127.0.0.1:9001
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 1: Docker NGINX 网关容器                                           │
│ 容器名: k8s-gateway                                                      │
│ 镜像:   nginx:latest                                                     │
│ 端口:   宿主 9001（--network host，容器 9001 = 宿主 9001）               │
│ 配置:   deploy/gateway/nginx.conf (挂载为 default.conf)                 │
│                                                                          │
│ location / {                                                             │
│     proxy_pass http://127.0.0.1:30000;  ← 转发到 K8s NodePort（--network host 下 127.0.0.1 即宿主机回环） │
│     proxy_set_header Host $host;                  ← 保留域名 Host 头      │
│ }                                                                        │
└────────────────────────────┬────────────────────────────────────────────┘
                             │ 127.0.0.1:30000
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 2: K8s ingress-nginx (NodePort Service + DaemonSet)                │
│                                                                          │
│ Service:                                                                 │
│   NAME                        TYPE       PORT(S)                         │
│   ingress-nginx-controller    NodePort   80:30000/TCP, 443:30443/TCP     │
│   ClusterIP: 10.96.35.7                                                  │
│                                                                          │
│ DaemonSet:                                                               │
│   NAME                       DESIRED  READY  NODE SELECTOR               │
│   ingress-nginx-controller   1        1      kubernetes.io/os=linux      │
│   → Pod: ingress-nginx-controller-585m7 (10.244.0.11)                    │
│   镜像: registry.k8s.io/ingress-nginx/controller:v1.11.3                 │
│                                                                          │
│ 作用: 接收 NodePort 30000 的流量，根据 Ingress 规则路由                    │
└────────────────────────────┬────────────────────────────────────────────┘
                             │ 匹配 Ingress host + path
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 3: K8s Ingress (prd namespace)                                     │
│                                                                          │
│ NAME          HOSTS                      ADDRESS      PORTS              │
│ k8s-console   k8s-cicd.daiyi.local.com   10.96.35.7   80                 │
│                                                                          │
│ 路由规则:                                                                 │
│   /api (Prefix)  → k8s-console-backend:8000   (ClusterIP Service)        │
│   /   (Prefix)  → k8s-console-frontend:80     (ClusterIP Service)        │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
              ┌──────────────┴──────────────┐
              ▼                              ▼
┌─────────────────────────┐  ┌─────────────────────────────┐
│ Backend Service          │  │ Frontend Service             │
│ k8s-console-backend      │  │ k8s-console-frontend         │
│ ClusterIP: 10.96.96.47   │  │ ClusterIP: 10.96.230.7       │
│ Port: 8000               │  │ Port: 80                     │
│ Selector:                │  │ Selector:                    │
│   app=k8s-console-backend │  │   app=k8s-console-frontend   │
└────────────┬──────────────┘  └────────────┬────────────────┘
             │                              │
             ▼                              ▼
┌─────────────────────────┐  ┌─────────────────────────────┐
│ Backend Pod              │  │ Frontend Pod                 │
│ Deployment: 1 replica    │  │ Deployment: 1 replica        │
│ Container: django        │  │ Container: nginx             │
│ Image: k8s-console-      │  │ Image: k8s-console-          │
│        backend:latest    │  │        frontend:latest       │
│ Port: 8000               │  │ Port: 80                     │
│                          │  │                              │
│ 启动流程 (Dockerfile):    │  │ 启动流程 (Dockerfile.local):  │
│ 1. migrate               │  │ 1. COPY dist → /usr/share/   │
│ 2. init_admin            │  │    nginx/html                │
│ 3. gunicorn :8000        │  │ 2. nginx -g daemon off       │
│                          │  │                              │
│ 外部依赖:                  │  │ 静态文件:                     │
│   MySQL (mysql.database   │  │   Vue SPA (index.html + JS) │
│   .svc:3306)             │  │   nginx try_files 路由       │
│   Redis (redis.database  │  │   Gzip 压缩                  │
│   .svc:6379)             │  │                              │
│                          │  │                              │
│ RBAC:                    │  │                              │
│   ServiceAccount:        │  │                              │
│   k8s-console            │  │                              │
│   ClusterRole:           │  │                              │
│   k8s-console            │  │                              │
│   (CRUD on K8s resources)│  │                              │
└─────────────────────────┘  └─────────────────────────────┘
```

## 二、链路层级速查

| 层级 | 组件 | 类型 | 地址 | 说明 |
|------|------|------|------|------|
| **L0** | 用户浏览器 | Windows 进程 | `k8s-cicd.daiyi.local.com:9001` | hosts 解析到 127.0.0.1，代理绕过 |
| **L1** | k8s-gateway | Docker 容器 | `0.0.0.0:9001 → :80` | nginx:latest，反向代理到 L2 |
| **L2** | ingress-nginx-controller | K8s DaemonSet + NodePort SVC | `:30000` (NodePort) | 集群流量入口，ingress-class: nginx |
| **L3** | k8s-console | K8s Ingress (prd) | host: `k8s-cicd.daiyi.local.com` | 路由分离: `/api` → backend, `/` → frontend |
| **L4** | k8s-console-backend | K8s ClusterIP SVC | `10.96.96.47:8000` | → Backend Pod (Django) |
| **L4** | k8s-console-frontend | K8s ClusterIP SVC | `10.96.230.7:80` | → Frontend Pod (Vue SPA) |
| **L5a** | Backend Pod | K8s Deployment (1/1) | Django + Gunicorn :8000 | 连接 MySQL (3306) + Redis (6379) |
| **L5b** | Frontend Pod | K8s Deployment (1/1) | nginx :80 静态文件 | Vue SPA try_files 路由 + Gzip |

## 三、K8s 资源清单（deploy/console/）

按 `kubectl apply -f deploy/console/` 部署时会依次创建：

| 文件 | 资源名 | Kind | 用途 |
|------|--------|------|------|
| `01-sa-rbac.yaml` | k8s-console | ServiceAccount | Backend Pod 的身份凭证 |
| | k8s-console | ClusterRole | K8s 资源的完整 CRUD 权限 |
| | k8s-console | ClusterRoleBinding | 将 SA 绑定到 ClusterRole |
| `02-configmap.yaml` | k8s-console-config | ConfigMap | DEBUG, MySQL/Redis 地址端口 |
| `03-secret.yaml` | k8s-console-secret | Secret | DJANGO_SECRET_KEY, DB 密码, Redis 密码 |
| `04-backend.yaml` | k8s-console-backend | Deployment (1) | Django 容器，gunicorn 4 workers |
| | k8s-console-backend | Service (ClusterIP) | 8000 → 8000 |
| `05-frontend.yaml` | k8s-console-frontend | Deployment (1) | nginx 容器，Vue SPA 静态文件 |
| | k8s-console-frontend | Service (ClusterIP) | 80 → 80 |
| `06-ingress.yaml` | k8s-console | Ingress | host + path 路由: `/api` + `/` |

### 镜像构建对照

| 组件 | 镜像 Tag | Dockerfile | 构建步骤 |
|------|----------|-----------|---------|
| Backend | `k8s-console-backend:latest` | `backend/Dockerfile` | 多阶段构建 (builder + runtime)，`pip install` → `gunicorn` |
| Frontend (本地) | `k8s-console-frontend:latest` | `frontend/Dockerfile.local` | 先 `npm run build`，再 `COPY dist` 到 nginx |
| Frontend (CI) | — | `frontend/Dockerfile` | 多阶段构建，`node:22-alpine` 编译 + `nginx:latest` 运行时 |

### ClusterRole 权限范围

```
Read:   namespaces, pods, services, configmaps, secrets, serviceaccounts,
        deployments, daemonsets, statefulsets, ingresses,
        roles, rolebindings, clusterroles, clusterrolebindings
Write:  pods, services, configmaps, secrets, serviceaccounts,
        deployments, daemonsets, statefulsets, ingresses,
        roles, rolebindings, clusterroles, clusterrolebindings, namespaces
Scale:  deployments/scale, statefulsets/scale
Rollback: deployments/rollback
```

## 四、前置条件

- [**数据库已部署**](./数据库部署指南.md)：MySQL + Redis 在 `database` namespace 运行中
- [**Ingress-NGINX 已部署**](./K8s集群使用指南.md#5-ingress-配置可选)：DaemonSet + NodePort Service (:30000) 运行中
- [**K8s Console 应用已部署**](../deploy/console/)：Backend + Frontend Deployment + Ingress 运行在 `prd` namespace
- Windows 11 / WSL2
- Docker Desktop（含 Kubernetes 1.34+，已启用）
- kubectl（通过 kubeconfig 连接 K8s 集群）
- **Git Bash（MINGW64）— `deploy-all.sh` 必须在 Git Bash 中执行，不能在 WSL 中执行**
- 端口 9001 未被占用

> ⚠️ **为什么 deploy-all.sh 不能在 WSL 中执行？** 见 [本地网关部署指南 § F8](本地网关部署指南.md#f8-deploy-allsh-在-wsl-中报-modulenotfound-rolluplinux-x64-gnu)

---

## 五、部署与启动

### 5.1 构建镜像（首次 / 代码变更后）

```bash
# Backend（多阶段构建，无需预装依赖）
DOCKER_BUILDKIT=0 docker build --pull=false -t k8s-console-backend:latest -f backend/Dockerfile backend/

# Frontend（先编译 Vue，再打包 nginx 镜像）
cd frontend && npm run build && cd ..
DOCKER_BUILDKIT=0 docker build --pull=false -t k8s-console-frontend:latest -f frontend/Dockerfile.local frontend/
```

> `DOCKER_BUILDKIT=0 --pull=false` 解决 Docker Hub 不可达时的构建问题。

### 5.2 部署 K8s 资源

```bash
kubectl apply -f deploy/console/
```

部署顺序无关紧要（K8s 自动按依赖处理），全部创建在 `prd` namespace。

验证：

```bash
kubectl get pods -n prd
# 预期: k8s-console-backend-xxx  1/1 Running
#       k8s-console-frontend-xxx  1/1 Running

kubectl get ingress -n prd
# 预期: ADDRESS 不为空
```

### 5.3 配置 Windows hosts

管理员权限编辑 `C:\Windows\System32\drivers\etc\hosts`：

```
127.0.0.1 k8s-cicd.daiyi.local.com
```

### 5.4 配置 Windows 代理绕过 ⚠️ 关键

如果 Windows 开启了系统代理（常见于 VPN/代理工具），Chrome 会把所有请求先发给代理，代理不理解自定义域名 → 返回 502。

```bash
# 1. 检查是否开启了代理
reg query "HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Internet Settings" //v ProxyEnable
reg query "HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Internet Settings" //v ProxyServer

# 2. 追加自定义域名到绕过列表
reg add "HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Internet Settings" //v ProxyOverride //t REG_SZ //d "原有值;*.daiyi.local.com" //f
```

或 GUI：设置 → 网络和 Internet → 代理 → 手动编辑请勿代理列表。

**配置后重启 Chrome。**

### 5.5 启动本地网关

```bash
bash deploy/gateway/start.sh
```

脚本自动：检查 NodePort → 拉取镜像 → 停止旧容器 → 启动新容器 → 验证 3 个端点。

### 5.6 验证

```bash
# 3 个健康端点
curl http://localhost:9001/ -H 'Host: k8s-cicd.daiyi.local.com'         # 前端页面
curl http://localhost:9001/api/health -H 'Host: k8s-cicd.daiyi.local.com'  # 后端
curl http://localhost:9001/gateway-health                                    # 网关自身

# 通过域名直达（需要代理绕过配置正确）
curl http://k8s-cicd.daiyi.local.com:9001/
```

浏览器访问：**http://k8s-cicd.daiyi.local.com:9001**

### 5.7 停止

```bash
bash deploy/gateway/stop.sh
```

---

## 六、故障排查

### F1: Pod `ImagePullBackOff`

镜像未构建或不在 registry 中。需要本地构建（见 5.1）。

```bash
# 检查镜像是否存在
docker images | grep k8s-console
```

### F2: 浏览器 502 — WSL wslrelay.exe 端口冲突（80 端口专属）

**现象**：curl 正常，浏览器 502。Docker 日志中看不到浏览器请求。

**根因**：WSL 的 `wslrelay.exe` 占用 IPv6 回环 `[::1]:80`，浏览器 IPv6 localhost 请求被拦截。

**解决**：本项目已改用 **9001 端口**规避。

**诊断**：`netstat -ano | findstr ':80.*LISTENING'` → PID 9612 = wslrelay.exe

### F3: 浏览器 502 — Windows 系统代理拦截

**现象**：
- `curl --noproxy '*' http://k8s-cicd.daiyi.local.com:9001/` → ✅ 200
- `curl --proxy 127.0.0.1:9674 http://k8s-cicd.daiyi.local.com:9001/` → ❌ 502
- Chrome 浏览器访问 → 502

**根因**：系统代理不理解 `k8s-cicd.daiyi.local.com` 域名。

**解决**：见 5.4 配置代理绕过，重启 Chrome。

### F4: Git Bash 路径翻译错误

**现象**：容器报错 `no such file or directory`。

**根因**：Git Bash (MSYS2) 把 `/etc/nginx/...` 翻译成 `C:\Program Files\Git\etc\nginx\...` Windows 路径。

**解决**：`start.sh` 已内置 `MSYS_NO_PATHCONV=1` + `$(pwd -W)`。

### F5: K8s Ingress 不生效

**检查链路**：

```bash
# 1. Ingress 状态（ADDRESS 不为空 = 正常）
kubectl get ingress -n prd

# 2. ingress-nginx 是否运行
kubectl get pods -n ingress-nginx
kubectl get svc -n ingress-nginx

# 3. 绕过所有代理，直连 NodePort 测试
curl --noproxy '*' http://localhost:30000/ -H 'Host: k8s-cicd.daiyi.local.com'
```

### F6: 后端连不上 MySQL / Redis

```bash
# 检查后端日志
kubectl logs -n prd -l app=k8s-console-backend --tail 50

# 检查 ConfigMap 和 Secret
kubectl get configmap -n prd k8s-console-config -o yaml
kubectl get secret -n prd k8s-console-secret -o yaml

# 确认数据库 Service 在 database namespace 运行
kubectl get svc -n database
```

### F7: NodePort 30000 不可达 / 网关 502

**现象**：
- 网关返回 `502 Bad Gateway`
- `docker exec k8s-gateway curl 127.0.0.1:30000` → Connection refused
- `curl localhost:9001` → 502

**根因**：网关容器以 `--network host` 运行，nginx 通过 `127.0.0.1:30000` 直连宿主机上的 Ingress NodePort。若 ingress-nginx 的 NodePort Service 异常、或 DaemonSet Pod 未处于 Running，则访问会 502。

**解决**：
```bash
# 1. 确认 NodePort Service 存在且监听 30000
kubectl get svc -n ingress-nginx ingress-nginx-controller
#    PORT(S): 80:30000/TCP, 443:30443/TCP

# 2. 确认 ingress-nginx DaemonSet Pod 处于 Running
kubectl get pods -n ingress-nginx

# 3. 绕过宿主机代理直连测试（宿主机 HTTP_PROXY 会拦截 FQDN，见 F8）
curl --noproxy '*' http://localhost:30000/ -H 'Host: k8s-cicd.daiyi.local.com'
#    期望 200

# 4. 若仍不可达，重启网关使 nginx.conf 重新加载并重新绑定 --network host
bash deploy/gateway/stop.sh && bash deploy/gateway/start.sh
```

> 💡 **历史方案（已废弃）**：早期版本使用 `--add-host=host.docker.internal:host-gateway` + `kubectl port-forward` 双保险。因容器内 `host.docker.internal` 同时解析 IPv4（`192.168.65.254`）与 IPv6（`fdc4:f303:9324::254`），而 NodePort 的 port-forward 仅 IPv4 监听，nginx 偶发选到 IPv6 → 502。现统一改用 `--network host` + `proxy_pass http://127.0.0.1:30000` 彻底规避（无需 `host.docker.internal`，也无需 `kubectl port-forward`）。

---

### F8: deploy-all.sh 在 WSL 中报 `MODULE_NOT_FOUND @rollup/rollup-linux-x64-gnu`

**现象**：
```
Step 2/6: 构建前端镜像
Error: Cannot find module @rollup/rollup-linux-x64-gnu.
npm has a bug related to optional dependencies.
Please try `npm i` again after removing both package-lock.json and node_modules directory.
```

**根因**：`node_modules` 是在 Windows 侧安装的（含 `@rollup/rollup-win32-x64-*`），WSL 中的 rollup 检测到 `process.platform = 'linux'`，尝试加载 `rollup-linux-x64-gnu` 但不存在。

**解决**：

1. **推荐**：在 **Git Bash (MINGW64)** 中执行脚本：
   ```bash
   # 右键项目目录 → Git Bash Here
   bash deploy/deploy_one_by_one/deploy-all.sh
   ```

2. 如果必须在 WSL 中执行，先重装 node_modules：
   ```bash
   cd frontend
   rm -rf node_modules package-lock.json
   npm install
   cd ..
   bash deploy/deploy_one_by_one/deploy-all.sh
   ```

3. script 中已内置 `npm install`（在 `npm run build` 前），会检测当前平台并安装正确的原生模块。但如果 `node_modules` 已存在且依赖声明未变，`npm install` 可能跳过，建议先 `rm -rf node_modules`。

---

## 七、设计决策

| 决策 | 原因 |
|------|------|
| 镜像 `nginx:latest` 而非 `openresty/openresty:jammy` | 更小、更快、功能足够 |
| 宿主端口 `9001` 而非 `80` | 避免 WSL wslrelay / IIS 抢 80 端口 |
| `--network host` + `proxy_pass http://127.0.0.1:30000` | 容器共享宿主机网络，`127.0.0.1` 即宿主机回环，直连 Ingress NodePort；无需 `host.docker.internal` / `--add-host` / `kubectl port-forward`，彻底规避 IPv4/IPv6 解析错位导致的 502 |
| 网关脚本独立于组件化部署 | 网关启停仍由 `deploy/gateway/start.sh` / `stop.sh` 负责，组件化部署入口为 `deploy/deploy_one_by_one/` |
| Backend `imagePullPolicy: Always` | 开发期镜像频繁更新，确保拉取最新 |
| Frontend `imagePullPolicy: IfNotPresent` | 静态文件变更少，减少拉取 |
| `Dockerfile.local` vs `Dockerfile` | `.local` 手动构建（先 `npm run build`），CI 版多阶段构建 |

## 八、文件清单

| 文件 | 用途 |
|------|------|
| `docs/本地网关部署指南.md` | 本文档 |
| `deploy/deploy_one_by_one/clean-all.sh` | 组件化一键清理所有资源（原 `deploy/clean-all.sh` 保留为 `deploy/clean-all.sh.bak`） |
| `deploy/deploy_one_by_one/deploy-all.sh` | 组件化一键部署全部（构建+数据库+Ingress+Console+注册集群+网关；原 `deploy/deploy-all.sh` 保留为 `deploy/deploy-all.sh.bak`） |
| `deploy/kubeconfigs/docker-desktop.yaml` | 本机 kubeconfig（部署后自动注册集群） |
| `deploy/console/01-namespace.yaml` | prd Namespace |
| `deploy/console/02-sa-rbac.yaml` | ServiceAccount + ClusterRole + ClusterRoleBinding |
| `deploy/console/03-configmap.yaml` | 后端 ConfigMap（DB/Redis 地址） |
| `deploy/console/04-secret.yaml` | 后端 Secret（密码、密钥） |
| `deploy/console/05-backend.yaml` | Backend Deployment + Service |
| `deploy/console/06-frontend.yaml` | Frontend Deployment + Service |
| `deploy/console/07-ingress.yaml` | Ingress 路由规则 |
| `backend/Dockerfile` | Backend 镜像（多阶段） |
| `frontend/Dockerfile` | Frontend CI 镜像（多阶段） |
| `frontend/Dockerfile.local` | Frontend 本地构建 |
| `deploy/gateway/nginx.conf` | 网关 NGINX 配置 |
| `deploy/gateway/start.sh` | 网关一键启动（`--network host` 模式，无需 port-forward） |
| `deploy/gateway/stop.sh` | 网关停止 |

---

## 更新记录

| 日期 | 变更 |
|------|------|
| 2026-07-11 | v1: Docker nginx + kubectl port-forward 方案 |
| 2026-07-11 | v2: 换 nginx 镜像 + 9001 端口，发现并修复 WSL relay + 系统代理两个根因 |
| 2026-07-11 | v3: 重命名中文文件名；补充 7 层完整链路架构图、K8s 资源清单、镜像构建对照、RBAC 权限范围 |
| 2026-07-25 | v4: 网关改用 `--network host` + `proxy_pass http://127.0.0.1:30000`（废弃 `host.docker.internal` + `kubectl port-forward` 双保险方案）；部署脚本拆为 `deploy/deploy_one_by_one/` 组件化入口 |
