# 2026-07-26 工作日志

## Prometheus 学习文档补充
- 在 `docs/prometheus-monitoring-learning.html` 新增「三、Pull 网络链路（与网关访问路径的区别）」一节：内联 SVG（指标从后端内存→集群网络→Prometheus TSDB 的包级链路，marker 用 arrow3 避免与已有 arrow/arrow2 冲突）+ 7 步拆解 + scrape vs 浏览器访问路径对比表 + 「scrape 不走网关」callout。
- 原 三~六 顺延为 四~七，目录同步更新；章节 id 不变（chain/access/pullpath/config/rules/status/keypoints）。
- 配色用文档自身调色板（蓝 #E6F1FB/#185FA5、琥珀 #FAEEDA/#BA7517、灰 #F1EFE8/#5F5E5A），因独立 HTML 无 Visualizer 的 c-blue/c-amber/c-gray CSS。
- 反复强调的核心知识点：pull 模型流量由 Prometheus 发起，scrape 是 Pod→Pod 集群内流量，不经过网关/Ingress/NodePort；网关仅用于人访问页面；服务发现(kubernetes_sd_configs)告知目标 PodIP。
